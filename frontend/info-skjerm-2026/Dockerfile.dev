FROM node:22-alpine
WORKDIR /app

# Install deps — source is mounted at runtime, not copied
COPY package*.json ./
RUN npm ci

EXPOSE 5173

# --host binds to 0.0.0.0 so the port is reachable outside the container
CMD ["npm", "run", "dev", "--", "--host"]
